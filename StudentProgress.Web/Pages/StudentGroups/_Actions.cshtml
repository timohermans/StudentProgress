@model IndexModel

<a id="newAdventure" href="#" hx-get="@Url.Page("Create")" hx-target="#actions" hx-swap="innerHTML swap:200ms settle:200ms">
  Create New
</a> or

@if (Model.CanImportGroups)
{
  <a asp-page="/Canvas/Courses/Index">Import from Canvas</a>
}